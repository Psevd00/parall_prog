# Лабораторная работа №4
## Параллельное перемножение матриц с CUDA

**Студент:** Астафьев Вадим Алексеевич
**Группа:** 6311-100503D

---

## 1 Цель работы

Модифицировать программу из лабораторной работы №1 для параллельной работы с использованием технологии CUDA. Провести эксперименты с разными размерами матриц и различными конфигурациями сетки блоков.

---

## 2 CUDA (Compute Unified Device Architecture)

CUDA — платформа параллельного программирования от NVIDIA, позволяющая использовать графические процессоры (GPU) для высокопроизводительных вычислений. В отличие от CPU (4-8 ядер), современные GPU содержат тысячи ядер, оптимизированных для параллельной обработки данных.

---

## 3 Описание алгоритма

### 3.1 Распределение данных

При умножении матриц C = A × B на GPU:
- Каждый поток вычисляет **один элемент** результирующей матрицы C
- Матрицы A и B копируются из оперативной памяти в видеопамять (VRAM)
- Результат вычислений копируется обратно в оперативную память

### 3.2 Схема работы

| Этап | Описание |
|------|----------|
| 1 | Чтение матриц A и B из файлов (CPU) |
| 2 | Копирование данных из RAM в VRAM (cudaMemcpy) |
| 3 | Запуск CUDA ядра на GPU |
| 4 | Копирование результата из VRAM в RAM |
| 5 | Сохранение результата в файл |

### 3.3 Конфигурация сетки блоков

Для матрицы размера N×N и размера блока B×B:
- `threadsPerBlock = (B, B)` — количество потоков в блоке
- `blocksPerGrid = (ceil(N/B), ceil(N/B))` — количество блоков в сетке

---

## 4 Исходный код программы

```cpp
#include <iostream>
#include <fstream>
#include <vector>
#include <chrono>
#include <cuda_runtime.h>

using namespace std;

// CUDA KERNEL - каждый поток вычисляет один элемент
__global__ void matrixMulKernel(const double* A, const double* B, double* C, int N) {
    int row = blockIdx.y * blockDim.y + threadIdx.y;
    int col = blockIdx.x * blockDim.x + threadIdx.x;
    
    if (row < N && col < N) {
        double sum = 0.0;
        for (int k = 0; k < N; k++) {
            sum += A[row * N + k] * B[k * N + col];
        }
        C[row * N + col] = sum;
    }
}

// Чтение матрицы из файла
vector<vector<double>> readMatrix(const string& filename, int& n) {
    ifstream file(filename);
    file >> n;
    vector<vector<double>> matrix(n, vector<double>(n));
    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++)
            file >> matrix[i][j];
    return matrix;
}

int main(int argc, char* argv[]) {
    if (argc < 4) {
        cout << "Usage: " << argv[0] << " <matrix1> <matrix2> <block_size>" << endl;
        return 1;
    }
    
    string fileA = argv[1];
    string fileB = argv[2];
    int blockSize = atoi(argv[3]);
    
    int n;
    auto A = readMatrix(fileA, n);
    auto B = readMatrix(fileB, n);
    
    cout << "Matrix size: " << n << "x" << n << endl;
    cout << "Block size: " << blockSize << "x" << blockSize << endl;
    
    // Конвертация в плоские массивы
    vector<double> h_A(n * n), h_B(n * n), h_C(n * n);
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            h_A[i * n + j] = A[i][j];
            h_B[i * n + j] = B[i][j];
        }
    }
    
    // Выделение памяти на GPU
    double *d_A, *d_B, *d_C;
    size_t bytes = n * n * sizeof(double);
    cudaMalloc(&d_A, bytes);
    cudaMalloc(&d_B, bytes);
    cudaMalloc(&d_C, bytes);
    
    // Копирование данных на GPU
    cudaMemcpy(d_A, h_A.data(), bytes, cudaMemcpyHostToDevice);
    cudaMemcpy(d_B, h_B.data(), bytes, cudaMemcpyHostToDevice);
    
    // Настройка сетки
    dim3 threads(blockSize, blockSize);
    dim3 blocks((n + blockSize - 1) / blockSize, 
                (n + blockSize - 1) / blockSize);
    
    // Прогрев GPU
    matrixMulKernel<<<blocks, threads>>>(d_A, d_B, d_C, n);
    cudaDeviceSynchronize();
    
    // Замер времени
    auto start = chrono::high_resolution_clock::now();
    matrixMulKernel<<<blocks, threads>>>(d_A, d_B, d_C, n);
    cudaDeviceSynchronize();
    auto end = chrono::high_resolution_clock::now();
    chrono::duration<double> duration = end - start;
    
    // Копирование результата обратно
    cudaMemcpy(h_C.data(), d_C, bytes, cudaMemcpyDeviceToHost);
    
    // Вывод результатов
    long long ops = 2LL * n * n * n;
    double gflops = ops / (duration.count() * 1e9);
    
    cout << "Execution time: " << duration.count() << " seconds" << endl;
    cout << "Performance: " << gflops << " GFLOPS" << endl;
    
    cudaFree(d_A);
    cudaFree(d_B);
    cudaFree(d_C);
    
    return 0;
}
```
## 5 Характеристики системы

### 5.1 CPU (собственная система)

| Параметр | Значение |
|----------|----------|
| Процессор | AMD Ryzen 5 4600H with Radeon Graphics |
| Количество ядер | 6 физических / 12 логических |
| Базовая частота | 3.00 ГГц |
| ОЗУ | 8 ГБ |

### 5.2 GPU (использовал Google Colab)

| Параметр | Значение |
|----------|----------|
| GPU | NVIDIA Tesla T4 |
| VRAM | 16 GB |
| CUDA версия | 13.0 |
| Compute Capability | 7.5 |

---

## 6 Результаты экспериментов

### 6.1 Время выполнения на GPU (секунды)

| Размер N | Block 8×8 | Block 16×16 | Block 32×32 |
|:--------:|:---------:|:-----------:|:-----------:|
| 200 | 0.2924 | 0.1906 | 0.1777 |
| 400 | 0.1763 | 0.1761 | 0.2217 |
| 800 | 0.2724 | 0.2399 | 0.2346 |
| 1200 | 0.3048 | 0.3105 | 0.2907 |
| 1600 | 0.3549 | 0.4002 | 0.3645 |
| 2000 | 0.4772 | 0.5561 | 0.5184 |

### 6.2 Сравнение CPU и GPU

| Размер N | CPU время (с) | GPU время (с) | Ускорение |
|:--------:|:-------------:|:-------------:|:---------:|
| 200 | 0.2165 | 0.1777 | 1.2× |
| 400 | 1.7656 | 0.1761 | 10.0× |
| 800 | 10.2694 | 0.2346 | 43.8× |
| 1200 | 36.1968 | 0.2907 | 124.5× |
| 1600 | 88.5648 | 0.3549 | 249.6× |
| 2000 | 315.82 | 0.4772 | **661.8×** |

---

## 7 Графики


## 8 Анализ результатов

### 8.1 Анализ времени выполнения

| Размер | Наблюдение |
|--------|------------|
| N=200 | Лучшее время на блоке 32×32 (0.178 с) |
| N=400 | Лучшее время на блоке 16×16 (0.176 с) |
| N=800 | Лучшее время на блоке 32×32 (0.235 с) |
| N=1200 | Лучшее время на блоке 32×32 (0.291 с) |
| N=1600 | Лучшее время на блоке 8×8 (0.355 с) |
| N=2000 | Лучшее время на блоке 8×8 (0.477 с) |

### 8.2 Анализ ускорения

**Ключевые наблюдения:**

**Для малых матриц (N=200-400):**
- Ускорение незначительное (1.2-10×)
- Накладные расходы на копирование данных сопоставимы с временем вычислений

**Для средних матриц (N=800-1200):**
- Ускорение достигает 124×
- Вычислительная работа начинает преобладать над накладными расходами

**Для больших матриц (N=1600-2000):**
- Максимальное ускорение до 662×
- GPU полностью раскрывает свой потенциал


### 8.3 Влияние размера блока

| Размер блока | Преимущества | Недостатки |
|--------------|--------------|-------------|
| 8×8 (64 потока) | Лучше для больших матриц (≥1600) | Меньшая занятость warp |
| 16×16 (256 потоков) | Хороший баланс для средних матриц | Умеренная занятость |
| 32×32 (1024 потока) | Лучше для малых матриц (≤400) | Может превышать лимиты регистров |

---

## 9 Выводы

В ходе выполнения лабораторной работы №4:

1. **Разработана параллельная версия** программы умножения матриц с использованием CUDA. Реализовано ядро, где каждый поток вычисляет один элемент результирующей матрицы.

2. **Проведено исследование** для матриц размером от 200 до 2000 с конфигурациями блоков 8×8, 16×16 и 32×32.

3. **Получены следующие результаты:**
   - Максимальное ускорение: **662×** (N=2000)
   - Минимальное время выполнения: **0.176 с** (N=400, блок 16×16)

4. **Выявлены закономерности:**
   - Для малых матриц (≤400) оптимален блок 32×32
   - Для больших матриц (≥1600) лучше работает блок 8×8
   - Ускорение растёт с увеличением размера задачи
   - Эффективность GPU максимальна на больших матрицах

5. **Сравнение с CPU:**
   - GPU на Tesla T4 оказался **в 662 раза быстрее** CPU (AMD Ryzen 5 4600H) для матрицы 2000×2000
   - Производительность GPU на 3 порядка выше производительности CPU для больших задач

Программа демонстрирует, что использование GPU с CUDA даёт колоссальное ускорение по сравнению с CPU и является эффективным решением для задач линейной алгебры.

---

